{
  "name": "adsforecast-platform",
  "version": "0.11.0",
  "private": true,
  "description": "AdsForecast — profit-first Meta Ads analytics, AI decision support, billing and customer operations",
  "scripts": {
    "setup": "npm install",
    "dev": "npm run dev:web",
    "backend:status": "npx supabase functions list && npx supabase migration list",
    "backend:serve": "npx supabase functions serve --env-file supabase/.env.local",
    "backend:db:push": "npx supabase db push",
    "backend:deploy": "npx supabase functions deploy backend-health workspace-bootstrap account-center billing-center stripe-webhook support-center admin-console meta-start meta-callback meta-accounts meta-connect meta-connection meta-campaigns ai-analyze ai-insights --use-api --jobs 4",
    "dev:web": "npx --yes serve -l 5173 .",
    "start:api": "npm run backend:serve"
  }
}
